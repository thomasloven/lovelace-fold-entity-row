default_config:

demo:

lovelace:
  mode: storage
  dashboards:
    lovelace-yaml:
      mode: yaml
      title: yaml
      filename: lovelace.yaml

group:
  light_group:
    name: Light group
    entities:
      - light.bed_light
      - light.ceiling_lights
      - light.kitchen_lights
