x-anchors:
  desc: &desc
    type: markdown
    style: |
      code {
        font-size: 8pt;
        line-height: normal;
        white-space: pre-wrap;
        }

title: Errors
cards:
  - type: entities
    entities:
      - type: custom:fold-entity-row
  - type: entities
    entities:
      - type: custom:fold-entity-row
        head:
          entity: light.bed_light
  - type: entities
    entities:
      - type: custom:fold-entity-row
        head:
          entity: light.bed_light
        entities:
  - type: entities
    entities:
      - type: custom:fold-entity-row
        open: true
        head:
          entity: light.no_light
        entities:
          - light.no_light
